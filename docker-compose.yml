version: '3.8'

services:
  nest-ea:
    build:
      context: .
      dockerfile: Dockerfile  # 指定 Dockerfile 路径
    ports:
      - "3002:3002"  # 映射宿主机的 3000 端口到容器的 3002 端口
    volumes:
      - .:/nest-ea  # 将当前目录挂载到容器的 /nest-ea 目录中，方便实时更新代码
    environment:
      - NODE_ENV=production  # 设置环境变量，表示运行在生产环境
    restart: always  # 容器崩溃后自动重启
    networks:
      - nest-ea-network  # 定义服务使用的网络

networks:
  nest-ea-network:
    driver: bridge  # 使用桥接网络模式
